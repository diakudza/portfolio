{% extends 'partials/base.html.twig' %}

{% block content %}
    <div class="grid grid-rows justify-items-center mt-10">
        <div class="w-full">
            {% set image = page.media.images |first %}
            {% if image %}
                {{ image.html('','','rounded-lg shadow-2xl max-w-md') | raw }}
            {% endif %}
        </div>
        <div class="mt-10">
            {{ page.content|raw }}
        </div>
    </div>
{% endblock %}
