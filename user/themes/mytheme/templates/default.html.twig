{% extends 'partials/base.html.twig' %}

{% block content %}
    <div class="grid grid-rows justify-items-center">
        <div class="">
            {% set image = page.media.images |first %}
            {% if image %}
                {{ image.html('','','rounded-lg shadow-2xl max-w-lg') | raw }}
            {% endif %}
        </div>
        <div class="mt-10">
            {{ page.content|raw }}
        </div>
    </div>
{% endblock %}
